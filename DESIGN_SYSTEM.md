# QQQMBar 设计系统

## 产品原则

QQQMBar 是一次计划决策，不是缩小版交易终端。界面固定回答五个问题：本期投多少、为什么、价格在哪里、账户能否执行、数据是否可信。所有内容必须来自 `snapshot-v2.json` 的真实字段或可复算结果；缺失、失败和过期必须显式呈现，不能用占位图或模拟值补齐。

## 信息顺序

1. 行动：本期金额、倍率、执行日期、确认状态。
2. 市场：最新价、30 日变化、成本偏离、P/E、价格与真实 B/S 成交。
3. 规则：趋势、VIX、CNN 情绪的当前值、所处区间和下一档距离。
4. 执行：QQQM 股数、市值、盈亏、可用资金、本期预留和后续覆盖。
5. 信任：行情日期、账户日期、校验、刷新错误和只读边界。

## 视觉语言

- 背景只有两层：系统窗口底色与一张连续决策纸张；主流程不使用悬浮卡片。
- 圆角只使用 `16 / 12 / 9 pt` 三档；主界面不堆叠独立仪表盘卡片。
- 主数字使用 rounded + monospaced digits；标题和说明使用系统字体。
- 强调色保持低饱和。青绿为当前行动与价格，绿色为多投/买入/通过，琥珀为少投/门槛/警告，珊瑚红为实际负值/卖出/异常。
- 每种颜色都必须同时配合文字、形状或位置。真实成交采用“成交价—细引导线—轴下成交摘要”的图表原生语法：同一美股交易日、同方向的成交按真实记录归组，引导线终点表达数量加权成交价，轴下显示日期、方向、笔数与实际成交总额；价格仅在悬停详情中显示。曲线上不再叠加成交圆点、字母、气泡或浮签。状态使用图标与文案，轨道始终按低值到高值排列。
- 浅色和深色分别调色，不通过简单反色或统一透明度生成。

## 数据与格式

- QQQM 数量：4 位小数。
- 价格与盈亏：2 位小数。
- 计划与资金：整数美元，统一 `$`。
- 百分比：决策摘要 1 位，持仓与价格差 2 位。
- 同一个指标在所有模块使用相同来源、精度、符号和颜色。
- EMA20 只由当前 30 日价格序列计算；买入／卖出只来自真实 QQQM 成交；没有卖出时显示“卖出 0”。

## 状态矩阵

| 状态 | 主界面 | 菜单栏 | 数据行为 |
| --- | --- | --- | --- |
| 正常待确认 | 校验通过 + 确认按钮 | Q + 计划语义圆点 | 使用当前有效快照 |
| 已确认 | 绿色已确认 | Q + 绿色圆点 | 只保存本地确认记录 |
| 正在刷新 | 进度符号 + 沿用说明 | 保持原计划状态 | 原子更新前继续显示旧快照 |
| 刷新失败 | 琥珀警告 + 快照日期 | Q + 琥珀圆点 | 保留上一份有效快照 |
| 校验差异 | 明确差异数量 | Q + 红色圆点 | 禁止用无效数据覆盖 |
| 完全不可用 | 恢复说明 + 重新读取 | Q + 红色圆点 | 不生成模拟数据 |

## 交互

- 菜单栏使用 `NSPopover.behavior = .transient`；点击桌面或其他 App 立即关闭，不使用普通窗口缩放动画。
- 主按钮按 Return 可确认；待确认是可执行状态，使用强调色文字、勾选符号和轻量强调色底，不得呈现为灰色禁用态；确认只写本地记录，不下单。
- 图表支持选择日期查看精确收盘价；完整规则只通过一个整行按钮展开。
- 自动行情刷新每天一次，目标时间为美股收盘后 15 分钟；手动刷新保留在菜单和设置页。

## 参考边界

- Apple Human Interface Guidelines — [Color](https://developer.apple.com/design/human-interface-guidelines/color)、[Charts](https://developer.apple.com/design/human-interface-guidelines/charts)、[Popovers](https://developer.apple.com/design/human-interface-guidelines/popovers/)。
- Apple Stocks for macOS — [Stocks User Guide](https://support.apple.com/guide/stocks/welcome/mac)。
- Famous Holdings — [famous-holdings.vercel.app](https://famous-holdings.vercel.app/)；只借鉴低饱和层级、有限强调色和数据优先的秩序，不复制网页卡片结构、字体或图形。
